export interface NavItem {
  label: string;
  href: string;
}

export interface Property {
  id: string;
  title: string;
  location: string;
  image: string;
}

export interface ServiceDetail {
  id: string;
  title: string;
  description: string;
  icon: string;
}
